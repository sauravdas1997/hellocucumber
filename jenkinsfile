pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo "Resolving Dependencies"
        bundle install
      }
    }
    stage('Run') {
      steps {
        echo "Running Cases"
      }
    }
  }
}