pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo "Resolving Dependencies"
        sh "rvm use 2.7.5"
      }
    }
    stage('Run') {
      steps {
        echo "Running Cases"
        sh "rake features"
      }
    }
  }
}