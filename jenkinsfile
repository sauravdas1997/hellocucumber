pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo "Resolving Dependencies"
        sh "bundle install"
      }
    }
    stage('Run') {
      steps {
        echo "Running Cases"
      }
    }
  }
}